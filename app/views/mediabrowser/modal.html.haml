.editing-mediabrowser-body
  .editing-mediabrowser-wrapper
    = render 'header'

    = render 'filter', obj_classes: @searchable_classes

    .editing-mediabrowser-items

  .editing-mediabrowser-inspector.hide

= render 'button_bar'
